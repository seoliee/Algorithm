package 백준.N과M3_15651;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Scanner;

public class Main {
	static int N, M;
	static int[] arr;
	static List<Integer> result;
	static boolean[] visited;
	
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		
		// 자연수 1~N까지의 수 중에서 M개 고른 수열 출력
		// 같은 수 여러 번 골라도 됨
		N = sc.nextInt();
		M = sc.nextInt();
		
		// 1~N까지의 수 저장하는 배열
		arr = new int[N];
		
		// arr에 1~N 배열 저장하기
		for(int i = 0; i < N; i++) {
			arr[i] = i+1;
		}
		
		// M개의 수 저장하는 리스트
		result = new ArrayList<>();
		// 1~N 중 방문했는지 체크하는 배열
		visited = new boolean[N];
		
		perm(0);
		
	}
	
	// 검사 메서드
	// 0번 인덱스부터 확인하니까 main에서는 인자로 0 넣음
	public static void perm(int idx) {
		// 종료 조건
		// result에 저장한 데이터의 인덱스가 M이 된 경우 종료
		// result의 크기는 M이므로 인덱스는 0 ~ M-1
		if(idx == M) {
			for(int i = 0; i < M; i++) {
				System.out.print(result.get(i) + " ");
			}
			System.out.println();
			return;
		}
		
		
		// 재귀 코드
		// arr의 처음부터 마지막까지 돌면서 하나씩 result에 추가
		for(int i = 0; i < N; i++) {
			result.add(arr[i]);  // arr의 i번 인덱스 값 result에 추가
			
		}
	}

}
